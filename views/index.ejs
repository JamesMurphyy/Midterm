<!DOCTYPE html>
<html lang="en">


<head>
  <title>Home Page</title>

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" type="text/css" />
  <link rel="stylesheet" href="/vendor/border-box.css" type="text/css" />
  <link rel="stylesheet" href="/styles/main.css" type="text/css" />
  <link rel="stylesheet" href="/styles/layout.css" type="text/css" />
  <link rel="stylesheet" href="/styles/myItems.css" type="text/css" />
  <link rel="stylesheet" href="/styles/popup.css" type="text/css" />

  <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
  <script type="text/javascript" src="/scripts/app.js"></script>
</head>
<%- include('partials/_header') %>
<main>
  <div class="body-header">
    <label id="description_text">
      <p>Buy and Sell</p>
      <p> Find All your needs</p>
      <h3>Popular categories</h3>
    </label>
  </div>
  <!-- filter form -->
  <div class="div_filter_form">
    <form method="GET" action="/search" class="filter-form">
      <h3>Price Filter</h3>
      <!-- <label>Minimum Price</label> -->
      <input type="number" name="minPrice" id="min-price" placeholder="Minimum Price"></input>
      <!-- <label>Maximum Price</label> -->
      <input type="number" name="maxPrice" id="max-price" placeholder="Maximum Price"></input>
      <button type="submit" value="submit" id="search-submit">Filter</button>
    </form></div>

  </container>
  <div class="addPost">
    <div>
      <h3>Featured Posts</h3>
    </div>
    <% for (let post of posts) { %>
    <div id = "textbox">
      <div class = "postInfo">
        <p id = "postTitle"> <%= post.title %></p>
        <p id = "postDescription"> <%= post.description %></p>
        <img id = "postImg" src="<%= post.photo_url %>"/>
      </div>
          <div class = "myItemsButtons">
            <input id = favouriteButton type="button" value="FAVOURITE">
            <input id = messageButton type="button" value="Send Message">
          </div>
        <footer>
          <p><%= ("$" + post.price / 100) %></p>
        </footer>
    </div>
    <% } %>
  </div>
</main>
  <footer>
    <p>Copyright &copy; <script> document.write(new Date().getFullYear()); </script>, Platform</p>

  </footer>
</body>
</html>


            <button type="submit" value="submit" id="search-submit">
              Filter
            </button>

          </form>
        </div>
        <!-- poted items -->
        <div class="item_grid">
          <% for (let post of posts) { %>
            <div class="item">
              <p class="post_title">
                <%= post.title %> post.title
              </p>

              <img class="item_img" src="<%= post.photo_url %>" alt="description:<%= post.description %>" />
              <!-- The Modal -->
              <footer class="item_footer">
                <p>
                  <%= ("$" + post.price / 100) %>
                </p>
              </footer>
            </div>
            <% } %>
        </div>
        <!-- many to one model -->
        <div id="myModal" class="modal">
          <span class="close">&times;</span>
          <img class="modal-content" id="img01" />
          <div id="caption">
          </div>
        </div>
      </main>
      <%- include('partials/_footer') %>
  </body>


</html>
